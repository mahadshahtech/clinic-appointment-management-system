import { LockKeyhole, LogOut, Mail, Phone, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "../components/page-header";
import { ProfileInfoRow } from "../components/profile-info-row";
import { useAuth } from "../features/auth/auth-provider";

export function AccountProfilePage(){const {profile,user,signOut}=useAuth();if(!profile)return null;const initials=profile.fullName.split(" ").filter(Boolean).map(part=>part[0]).slice(0,2).join("");return <div className="profile-page"><PageHeader eyebrow="Account and identity" title="Your profile" subtitle="Verified clinic identity and secure account information." icon={UserRound}/><section className="profile-hero glass-panel"><div className="profile-hero-avatar" aria-hidden="true">{initials}</div><div className="profile-hero-copy"><div className="profile-badges"><span className="role-badge"><ShieldCheck size={13}/>{profile.role.charAt(0)+profile.role.slice(1).toLowerCase()}</span><span className="account-badge"><i/>Active account</span></div><h2>{profile.fullName}</h2><p><Mail size={15}/>{user?.email??"Email unavailable"}</p><p><Phone size={15}/>{profile.phone||"Not added"}</p></div></section><div className="profile-layout single-profile-layout"><section className="content-surface glass-panel"><div className="section-heading"><div><p>Account information</p><h2>Verified details</h2></div><UserRound/></div><div className="profile-info-grid"><ProfileInfoRow icon={UserRound} label="Full name" value={profile.fullName}/><ProfileInfoRow icon={Mail} label="Email" value={user?.email}/><ProfileInfoRow icon={Phone} label="Phone" value={profile.phone}/><ProfileInfoRow icon={ShieldCheck} label="Role" value={profile.role}/><ProfileInfoRow icon={ShieldCheck} label="Account status" value={profile.isActive?"Active":"Inactive"}/></div></section><section className="content-surface security-card glass-panel"><div className="section-heading"><div><p>Account & security</p><h2>Secure access</h2></div><LockKeyhole/></div><p>Password and session security are managed by Supabase Auth. Sign out when using a shared device.</p><button className="button button-secondary" onClick={()=>void signOut()}><LogOut size={16}/>Sign out</button></section></div></div>;}
